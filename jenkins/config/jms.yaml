unclassified:
  jmsProviders:
    configs:
      - rabbitMq:
          name: "Fedora Messaging"
          hostname: "rabbitmq.fedoraproject.org"
          portNumber: 5671
          virtualHost: "/public_pubsub"
          exchange: "amq.topic"
          authenticationMethod:
            sslCertificate:
              keyStoreCredentialId: "fedora-messaging-keystore"
              trustStoreCredentialId: "fedora-messaging-truststore"